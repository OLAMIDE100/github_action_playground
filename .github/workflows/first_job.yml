name: 'first_job'

on:
  push:
    branches:
     - "main/cloudfunction"
  

permissions:
  contents: read

jobs:
  first_job:
    name: 'testing_git_action'
    runs-on: ubuntu-latest
    environment: production

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    steps:
    - name: Call my name
      uses: actions/checkout@v4
    
    - name: check files
      run: ls 


  second_job:
    needs: first_job
    name: 'testing_git_action_2'
    runs-on: ubuntu-latest
    environment: production

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    steps:
    - name: Call my name
      run: echo "${{vars.MY_NAME}}"